name: Keep Render Awake

on:
  schedule:
    # Runs every 5 minutes
    - cron: "*/5 * * * *"

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render Backend
        run: |
          curl -L --fail https://rastaflow-1.onrender.com/health